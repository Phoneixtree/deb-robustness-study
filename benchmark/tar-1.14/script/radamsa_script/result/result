tar-archives-multiple-files count = 500 

reduced_bin -cf hang 0 times (total 500)

reduced_bin -cf has 14 exceptions (total 500)

reduced_bin successfully executed 0 times (total 1000)

reduced_bin -xf hang 0 times

reduced_bin -xf has 0 exceptions

tar-complains-about-missing-file count = 1 

reduced_bin -cf hang 0 times

reduced_bin -cf has 0 exceptions 

reduced_bin -xf hang 0 times

reduced_bin -xf has 0 exceptions

tar-extracts-to-standard-output count = 500 

reduced_bin -cf hang 0 times (total 500)

reduced_bin -cf has 8 exceptions (total 500)

reduced_bin successfully executed 0 times (total 500)

tar-handles-cz-options count = 500 

reduced_bin -czf hang 0 times (total 500)

reduced_bin -czf has 8 exceptions (total 500)

reduced_bin successfully executed 0 times (total 500)

reduced_bin -xzf hang 0 times (total 500)

reduced_bin -xzf has 0 exceptions (total 500)

tar-handles-exclude-and-extract-lists count = 500 

reduced_bin -xf hang 0 times (total 500)

reduced_bin -xf has 501 exceptions (total 500)

reduced_bin successfully executed 0 times (total 500)

